# CodeChronos environment
# Copy to .env.local and fill in the keys you have.

# --- Required for AI answers and for reconstructing non-seed videos ---
# https://console.x.ai
XAI_API_KEY=
# Cheap default is grok-4-1-fast-non-reasoning (~$0.20/$0.50 per 1M).
# Set grok-4.6 only if you want higher-quality explanations.
XAI_MODEL=grok-4-1-fast-non-reasoning

# --- Optional: secure multi-language sandbox (https://e2b.dev) ---
# Without this, Python runs in Pyodide and JS in a Web Worker.
E2B_API_KEY=

# --- Screen extract ---
# ffmpeg + tesseract (already common on macOS via brew)
# pip3 install yt-dlp pillow
# Deno (for YouTube JS challenges): curl -fsSL https://deno.land/install.sh | sh
# First Open-editor downloads the video once (~55MB at 360p) using your Chrome cookies,
# then every later timestamp is a local ffmpeg seek + OCR. No per-frame API.
# XAI_API_KEY is only for cleaning that OCR and for Query/Understand.
YTDLP_COOKIES_FROM_BROWSER=chrome
