"use client";

/**
 * Featured seeds are complete. Other videos use live screen reads plus the
 * local media cache. The old LLM horizon invented code — do not call it.
 */
export function useHorizon(_videoId: string) {}
